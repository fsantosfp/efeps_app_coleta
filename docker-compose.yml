version: '3.8'

services:
  app:
    build: .
    container_name: efeps_app_coleta_dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    restart: always
